Love that the essentials are in place so fast.

Yes, this is all the core essential functionality and everything worked for me.

I was able to create producers and sequestration sites and logged KG of garbage for both. I also was able to see everything i had logged in the transactions list.

So let's do these features next so we can get it into the facility:

1. I need to be able to delete or edit feedstock suppliers/sequestration sites, and have all their transactions removed once they are deleted.

2. I like the up arrow for adding more KG but it is too small to click. I also want another arrow for 1/10th of a KG increments to enter more precise numbers. 

3. Default the web app to the screen for entering the most recently logged transaction. If supplier A was the most recently logged transaction, when we load the web app it should assume we are going to log more from supplier A. We'll be entering 100 entries ore more for the exact same feedstock supplier so being kicked out of that entry page with every entry will slow our team down too much.

4. Every time it says "producers," change that to "feedstock suppliers."

5. The tab for "feedstock suppliers" and "sequestration sites" is good, but let's combine the other 3 tabs into one view called "logging," and make that the default view of the web app. A dropdown picker will select between "incoming" and "outgoing," and the transactions will appear below a line that separates the data entry area from the list of transactions below it. 

6. Let's add timestamps and transaction IDs to each recording as well.

7. On the feedstock suppliers tab, display 3 metrics next to each supplier: today's total, total for the last calendar month, and total since the first of this month.

8. Let any one of those 3 metrics be the sorting factor for the list, defaulting to "total since 1st of month"

Once that's in place we'll be able to manually create invoices from this tool by looking at last month's totals, maintain a handle on our monthly performance, and have a good easy to use data entry dashboard.

Eventually, let's evolve this to take the load cell data from our IoT device to automatically build an invoice complete with transaction log reports. This will allow for a single-click end-of-month process and enable payment logging so we can track earnings and collections. I also imagine we can add logins so suppliers and sequestration sites can access this portal. They would see a stripped-down view showing only their transaction history for auditing purposes, without the ability to log data or see others' information.

This is coming together really great and I'm very excited to see it take shape. Let me know when items 1 - 8 above are ready for review. Of coruse we'll address the future concepts after that.