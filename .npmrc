# Without this, an install on a Node below the engines floor succeeds and the
# test run then dies inside a transitive dependency with an ERR_REQUIRE_ESM
# trace that never names the real cause.
engine-strict=true
